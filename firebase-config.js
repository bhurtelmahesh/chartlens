// Replace this with your Firebase web app config after you create the Firebase app.
// The app works without it, but login/signup and cloud sync stay disabled.
window.CHARTLENS_FIREBASE_CONFIG = null;
