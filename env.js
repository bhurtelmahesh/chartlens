// Runtime environment for ChartLens.
// Extracted from an inline <script> so the pages can ship a strict
// Content-Security-Policy without needing 'unsafe-inline' or hashes.
window.CHARTLENS_FIREBASE_CONFIG = window.CHARTLENS_FIREBASE_CONFIG || null;
window.CHARTLENS_API_BASE = window.CHARTLENS_API_BASE || 'https://chartlens-market-proxy.chartlens101.workers.dev';
